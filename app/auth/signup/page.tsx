import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Cadastro - Estética Flowers',
  description: 'Crie sua conta Estética Flowers',
};

export default function SignupPage() {
  return <SignupForm />;
}
